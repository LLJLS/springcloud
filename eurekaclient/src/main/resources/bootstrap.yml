spring:
  application:
    name: eurekaclient
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8101
      fail-fast: true
      enabled: true
      label: master
      profile: dev
